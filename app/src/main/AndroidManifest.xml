<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cfmg.chucknorris">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cartoon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_cartoon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChuckNorris">
        <activity
            android:name="com.cfmg.chucknorris.SettingsAndHistoricalQuotes"
            android:exported="true" />
        <activity
            android:name="com.cfmg.chucknorris.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>